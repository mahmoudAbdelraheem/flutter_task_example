import 'package:flutter/material.dart';

class TaskTwoScreen extends StatelessWidget {
  const TaskTwoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Task Two Screen'),
      ),
    );
  }
}
